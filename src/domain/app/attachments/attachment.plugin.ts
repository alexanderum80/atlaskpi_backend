// Not needed for now. I am going to have the attachments collection independent

// import * as mongoose from 'mongoose';
// import { AttachmentSchema } from './attachment-model';

// export function attachmentPlugin(schema: mongoose.Schema, options?: any) {
//     schema.path('attachments', [AttachmentSchema]);

//     schema.statics.addAttachment = addAttachment;
//     schema.statics.removeAttachment = removeAttachment;
//     schema.statics.getAttachment = getAttachment;
// }

// function addAttachment(name: string, md5: string, data: Buffer) {
    
// }

// function removeAttachment(id: string) {
        
// }

// function getAttachment(id: string) {

// }